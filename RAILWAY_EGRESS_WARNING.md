# ⚠️ Предупреждение Railway о Egress Fees

## Что это значит?

Railway показывает предупреждение:
```
Connecting to a public endpoint will incur egress fees.
```

## ✅ Это нормально для нашего проекта!

### Почему нужен PUBLIC_DOMAIN?

1. **Backend должен быть доступен публично:**
   - Bot (другой сервис) обращается к Backend API
   - MiniApp (Vercel) обращается к Backend API
   - Пользователи через Telegram обращаются к Backend

2. **PRIVATE_DOMAIN не подойдет:**
   - Private domain доступен только внутри Railway
   - MiniApp на Vercel не сможет достучаться до Backend
   - Bot может, но MiniApp - нет

### О Egress Fees

**Что это:**
- Плата за исходящий трафик из Railway
- Взимается когда ваш сервис отправляет данные наружу

**В нашем случае:**
- Backend **получает** запросы (incoming)
- Backend **отвечает** на запросы (outgoing)
- Это минимальный трафик для ответов API
- На бесплатном тарифе Railway обычно достаточно лимита

**Когда будут большие fees:**
- Если Backend сам делает много запросов наружу
- Если Backend скачивает большие файлы
- В нашем случае этого нет - Backend только отвечает

### Что делать?

1. **Используйте `RAILWAY_PUBLIC_DOMAIN`** ✅
   - Это правильно для нашего случая
   - Backend должен быть публично доступен

2. **Игнорируйте предупреждение** ✅
   - Нажмите "Continue" или "OK"
   - Это нормальное предупреждение

3. **Не переключайтесь на PRIVATE_DOMAIN** ❌
   - Это сломает доступ из MiniApp
   - MiniApp на Vercel не сможет достучаться до Backend

## 📊 Сравнение

| Тип | Доступность | Использование |
|-----|-------------|---------------|
| **PUBLIC_DOMAIN** | Доступен извне | ✅ Нужен для Bot и MiniApp |
| **PRIVATE_DOMAIN** | Только внутри Railway | ❌ MiniApp не сможет подключиться |

## 💡 Итог

**Используйте `RAILWAY_PUBLIC_DOMAIN` для `BACKEND_PUBLIC_URL`!**

Это правильный выбор для нашего проекта. Предупреждение можно игнорировать - egress fees будут минимальными, так как Backend только отвечает на запросы, а не делает много исходящих соединений.

