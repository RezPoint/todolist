# 🚀 Быстрый старт

## ✅ Токен бота настроен

Токен для бота **@todolistrezpoinbot** успешно добавлен в конфигурацию.

## 📋 Шаги для запуска

### 1. Запустите базу данных

**Вариант A: Docker Compose**
```bash
docker-compose up -d postgres redis
```

**Вариант B: Локальный PostgreSQL**
- Убедитесь, что PostgreSQL запущен
- База данных должна быть доступна по адресу из `apps/backend/.env`

### 2. Примените миграции

```bash
npm run prisma:migrate -w apps/backend
npm run prisma:generate -w apps/backend
```

### 3. Запустите Backend

```bash
npm run dev -w apps/backend
```

Backend будет доступен на: http://localhost:4000
- Health check: http://localhost:4000/api/health
- Swagger UI: http://localhost:4000/docs

### 4. Запустите Bot

В новом терминале:
```bash
npm run dev -w apps/bot
```

Бот должен подключиться к Telegram и начать отвечать на команды.

### 5. (Опционально) Запустите MiniApp

В новом терминале:
```bash
npm run dev -w apps/miniapp
```

MiniApp будет доступен на: http://localhost:5173

## 🧪 Проверка работы

### Проверка Backend
```bash
curl http://localhost:4000/api/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"2025-11-28T..."}
```

### Проверка Bot

1. Найдите бота в Telegram: **@todolistrezpoinbot**
2. Отправьте команду `/start`
3. Бот должен ответить приветствием

### Команды бота

- `/start` - начать работу
- `/add <задача>` - добавить задачу
- `/list` - показать все задачи
- `/done <номер>` - отметить задачу выполненной
- `/todo <номер>` - вернуть задачу в работу
- `/delete <номер>` - удалить задачу
- `/today` - задачи на сегодня

Также можно просто написать текст - бот автоматически создаст задачу!

## ⚠️ Важно

1. **Backend должен быть запущен** перед запуском бота
2. **База данных должна быть настроена** и миграции применены
3. **Проверьте DATABASE_URL** в `apps/backend/.env`

## 🔧 Решение проблем

### Бот не отвечает
- Убедитесь, что backend запущен и доступен
- Проверьте логи бота на наличие ошибок
- Проверьте, что токен правильный в `apps/bot/.env`

### Backend не запускается
- Проверьте, что PostgreSQL запущен
- Проверьте DATABASE_URL в `.env`
- Убедитесь, что миграции применены

### Ошибки подключения к БД
- Проверьте, что PostgreSQL доступен
- Проверьте учетные данные в DATABASE_URL
- Убедитесь, что база данных `todolist` создана

## 📞 Поддержка

Если возникли проблемы, проверьте:
- Логи backend: вывод в терминале где запущен `npm run dev -w apps/backend`
- Логи bot: вывод в терминале где запущен `npm run dev -w apps/bot`
- Файлы конфигурации: `.env` файлы в `apps/backend/` и `apps/bot/`

