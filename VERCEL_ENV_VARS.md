# 🔧 Добавление переменных окружения в Vercel

## ❌ Проблема

Ошибка при добавлении переменной в Team Settings:
```
The name contains invalid characters...
```

## ✅ Решение

### Способ 1: Добавить в настройках проекта (Рекомендуется) ⭐

1. **Откройте Vercel Dashboard**
2. **Найдите ваш проект** (например: `miniapp` или `todolist-miniapp`)
3. **Откройте проект**
4. **Перейдите в Settings → Environment Variables**
5. **Добавьте переменную:**
   - Key: `VITE_BACKEND_URL`
   - Value: `https://backend-production-26ea.up.railway.app`
   - Environments: выберите `Production`, `Preview`, `Development` (или все)
6. **Нажмите Save**

### Способ 2: Использовать Team Variables

Если вы находитесь в Team Settings:

1. **В разделе "Link To Projects"**
   - Нажмите "Search for a Project to link to..."
   - Найдите и выберите ваш проект `miniapp`
   - Это свяжет переменную с проектом

2. **Проверьте переменную:**
   - Key: `VITE_BACKEND_URL` ✅ (правильное имя)
   - Value: `https://backend-production-26ea.up.railway.app` ✅

3. **Нажмите Save**

4. **После сохранения:**
   - Переменная будет доступна в проекте
   - Сделайте redeploy проекта

## 📋 Переменные для MiniApp

Добавьте в проект `miniapp`:

```
VITE_BACKEND_URL=https://backend-production-26ea.up.railway.app
```

## ✅ Проверка

После добавления переменной:

1. **Сделайте redeploy:**
   - Vercel Dashboard → ваш проект → Deployments
   - Нажмите "..." на последнем деплое → Redeploy
   - Или выполните: `cd apps/miniapp && vercel --prod`

2. **Проверьте что переменная доступна:**
   - В логах деплоя не должно быть ошибок
   - MiniApp должен подключаться к Backend

## ⚠️ Важно

- **Имя переменной:** `VITE_BACKEND_URL` - правильное (начинается с `VITE_` для Vite проектов)
- **Значение:** URL Backend из Railway
- **Environments:** выберите где использовать (Production, Preview, Development)

## 🔍 Где найти проект

Если не видите проект в списке:

1. Проверьте что вы задеплоили MiniApp:
   ```bash
   cd apps/miniapp
   vercel --prod
   ```

2. Проект должен появиться в Vercel Dashboard

3. Имя проекта обычно: `miniapp` или `todolist-miniapp`

