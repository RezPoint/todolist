# 🚀 Финальный запуск миграций

## ❌ Проблема

Миграции не запустились автоматически при деплое.

## ✅ Решение: Запустить вручную через Railway Shell

### Шаг 1: Откройте Railway Shell

1. **Откройте Railway Dashboard**
2. **Откройте Backend сервис**
3. **Нажмите на три точки (...) в правом верхнем углу**
4. **Выберите "Shell"**

### Шаг 2: Выполните команды

В открывшемся терминале выполните:

```bash
cd /app/apps/backend
npx prisma migrate deploy
```

### Шаг 3: Проверьте результат

После выполнения должно быть:
```
✅ Applied migration: 20251128064806_init
```

### Шаг 4: Проверьте таблицы

1. **Railway → Postgres → Database → Data**
2. **Должны появиться таблицы:**
   - `User`
   - `Project`
   - `Task`
   - `ProjectCollaborator`
   - `_prisma_migrations`

## 🔄 Альтернатива: Redeploy

Если Shell не работает:

1. **Railway → Backend → Deployments**
2. **Нажмите "..." на последнем деплое**
3. **Выберите "Redeploy"**
4. **Дождитесь завершения деплоя**
5. **Проверьте логи - должны быть миграции**

## ⚠️ Если миграции не запускаются

### Проверьте DATABASE_URL

1. **Backend → Variables**
2. **Проверьте `DATABASE_URL`:**
   - Должен быть установлен
   - Должен начинаться с `postgresql://`
   - Формат: `${{Postgres.DATABASE_URL}}`

### Проверьте подключение Postgres

1. **Architecture view**
2. **Убедитесь что Postgres подключен к Backend**
3. **Должна быть линия между сервисами**

## ✅ После успешных миграций

1. **Backend должен запуститься**
2. **Bot сможет подключиться к Backend**
3. **Все должно работать!**

