# 🚀 Автоматическая настройка MiniApp

## ⚠️ Проблема

Localtunnel требует пароль, что не работает с Telegram MiniApp.

## ✅ Решение: Настройка через @BotFather

Это самый простой способ! Кнопка будет в меню бота и не требует пароля.

### Быстрая настройка:

1. **Запустите MiniApp** (если еще не запущен):
   ```bash
   npm run dev -w apps/miniapp
   ```

2. **Настройте публичный доступ** (выберите один вариант):

   **Вариант A: ngrok (рекомендуется для тестирования)**
   ```bash
   # 1. Зарегистрируйтесь на https://ngrok.com (бесплатно)
   # 2. Получите токен авторизации
   # 3. Авторизуйте:
   ngrok config add-authtoken YOUR_TOKEN
   
   # 4. Запустите туннель:
   ngrok http 5173
   
   # 5. Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
   ```

   **Вариант B: Загрузите на хостинг**
   - Vercel: `npm install -g vercel && vercel --prod` в папке apps/miniapp
   - Netlify: загрузите папку `apps/miniapp/dist` после `npm run build`
   - Или любой другой статический хостинг

3. **Настройте через @BotFather:**
   - Откройте @BotFather в Telegram
   - Отправьте `/mybots`
   - Выберите @todolistrezpoinbot
   - Выберите "Bot Settings" → "Menu Button"
   - Выберите "Configure Menu Button"
   - Введите текст: "📱 Открыть задачи"
   - Введите URL вашего MiniApp (HTTPS обязательно!)

4. **Готово!** Кнопка MiniApp появится в меню бота (внизу экрана)

## 🎯 Преимущества

- ✅ Не требует пароля
- ✅ Работает надежно
- ✅ Кнопка всегда видна в меню
- ✅ Не нужно обновлять код

## 📱 После настройки

Кнопка будет доступна:
- В меню бота (внизу экрана в Telegram)
- Всегда видна, не нужно отправлять /start

