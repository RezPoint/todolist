# 🌐 Настройка кастомного домена cryptalis.su

Инструкции по настройке домена `cryptalis.su` для всех компонентов проекта.

## 📋 Структура доменов

Рекомендуемая структура:
- **Backend API**: `api.cryptalis.su`
- **MiniApp**: `app.cryptalis.su` или `cryptalis.su`
- **Bot**: использует Backend URL

## 🚂 Railway (Backend)

### 1. Настройка домена

1. В Railway Dashboard откройте ваш Backend сервис
2. Перейдите в раздел **Settings** → **Networking**
3. Нажмите **Generate Domain** (если еще не создан)
4. Нажмите **Custom Domain**
5. Введите: `api.cryptalis.su`
6. Railway покажет DNS записи для настройки

### 2. Настройка DNS

В панели управления вашего домена добавьте CNAME запись:

```
Type: CNAME
Name: api
Value: [ваш-railway-домен].railway.app
TTL: 3600 (или Auto)
```

Или используйте A-запись, если Railway предоставит IP адрес.

### 3. Обновление переменных окружения

После настройки домена обновите переменную:

```
BACKEND_PUBLIC_URL=https://api.cryptalis.su
```

## ⚡ Vercel (MiniApp)

### 1. Настройка домена

1. В Vercel Dashboard откройте ваш проект MiniApp
2. Перейдите в **Settings** → **Domains**
3. Добавьте домен: `app.cryptalis.su` (или `cryptalis.su`)
4. Vercel покажет DNS записи для настройки

### 2. Настройка DNS

Добавьте CNAME запись:

```
Type: CNAME
Name: app (или @ для корневого домена)
Value: cname.vercel-dns.com
TTL: 3600
```

### 3. Обновление переменных окружения

В Vercel Dashboard → Settings → Environment Variables:

```
VITE_BACKEND_URL=https://api.cryptalis.su
```

## 🤖 Bot (Railway)

### Обновление переменных окружения

В Railway Dashboard для Bot сервиса обновите:

```
BACKEND_URL=https://api.cryptalis.su
MINIAPP_URL=https://app.cryptalis.su
```

## 🔧 Настройка Telegram Bot

### Обновление MiniApp URL

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **Bot Settings** → **Menu Button**
5. Введите URL: `https://app.cryptalis.su`
6. Или используйте команду:
   ```
   /setmenubutton
   [ваш бот]
   https://app.cryptalis.su
   ```

## ✅ Проверка

После настройки проверьте:

1. **Backend**: `https://api.cryptalis.su/api/health`
   - Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **MiniApp**: `https://app.cryptalis.su`
   - Должен открыться интерфейс MiniApp

3. **Bot**: Отправьте `/start` боту
   - Должна появиться кнопка для открытия MiniApp

## 🔒 SSL/HTTPS

- **Railway**: Автоматически выдает SSL сертификат через Let's Encrypt
- **Vercel**: Автоматически выдает SSL сертификат

Оба сервиса автоматически настроят HTTPS для вашего домена.

## 📝 DNS записи (пример)

В панели управления доменом `cryptalis.su`:

```
Type    Name    Value                          TTL
CNAME   api     [railway-domain].railway.app   3600
CNAME   app     cname.vercel-dns.com           3600
```

Или если используете корневой домен для MiniApp:

```
Type    Name    Value                          TTL
CNAME   api     [railway-domain].railway.app   3600
CNAME   @       cname.vercel-dns.com           3600
```

## ⚠️ Важно

1. **Распространение DNS**: Может занять до 48 часов, обычно 1-2 часа
2. **SSL сертификаты**: Выдаются автоматически после настройки DNS
3. **Проверка**: Используйте `dig` или онлайн инструменты для проверки DNS

## 🛠️ Проверка DNS

```bash
# Проверка CNAME записи для API
dig api.cryptalis.su CNAME

# Проверка CNAME записи для App
dig app.cryptalis.su CNAME
```

Или используйте онлайн сервисы:
- https://dnschecker.org
- https://www.whatsmydns.net

