# 🔧 Настройка переменных окружения в Railway

## ✅ Что нужно сделать СЕЙЧАС

### 1. Добавить предложенные переменные

Railway автоматически нашел переменные в вашем коде. Нужно добавить:

#### ✅ DATABASE_URL
- **Уже правильно настроено!** 
- Значение: `${{Postgres.DATABASE_URL}}`
- Это Reference на вашу PostgreSQL базу данных
- ✅ Оставьте как есть

#### ✅ BACKEND_PUBLIC_URL
- Нажмите на поле "BACKEND_PUBLIC_URL"
- Вместо "VALUE or ${{REF}}" выберите Reference:
  - Нажмите на иконку `{}` справа
  - Выберите `RAILWAY_PUBLIC_DOMAIN`
- Или введите вручную: `${{RAILWAY_PUBLIC_DOMAIN}}`

**⚠️ Важно:** Railway покажет предупреждение о egress fees. Это нормально!
- Backend должен быть **публично доступен** для Bot и MiniApp
- Используйте `RAILWAY_PUBLIC_DOMAIN` (не private)
- Egress fees будут минимальными, так как Backend только отвечает на запросы
- Это необходимо для работы проекта

#### ❌ TELEGRAM_BOT_TOKEN
- **НЕ добавляйте для Backend!**
- Эта переменная нужна только для Bot сервиса
- Нажмите на `X` справа, чтобы скрыть/удалить

#### ⚠️ REDIS_URL
- **Опционально** (не обязательно)
- Если не используете Redis, можно не добавлять
- Нажмите на `X` справа, чтобы скрыть

### 2. Добавить переменные

1. **Настройте BACKEND_PUBLIC_URL:**
   - Нажмите на поле `BACKEND_PUBLIC_URL`
   - Нажмите на иконку `{}` (Reference)
   - Выберите `RAILWAY_PUBLIC_DOMAIN`
   - Или введите: `${{RAILWAY_PUBLIC_DOMAIN}}`

2. **Удалите ненужные:**
   - Нажмите `X` на `TELEGRAM_BOT_TOKEN` (не нужен для Backend)
   - Нажмите `X` на `REDIS_URL` (если не используете)

3. **Нажмите кнопку "✓ Add"**
   - Это добавит настроенные переменные

### 3. Итоговый список переменных для Backend

После настройки у вас должно быть:

```
NODE_ENV=production (уже есть)
PORT=3000 (уже есть)
DATABASE_URL=${{Postgres.DATABASE_URL}} (добавить)
BACKEND_PUBLIC_URL=${{RAILWAY_PUBLIC_DOMAIN}} (добавить)
```

### 4. Деплой

1. После добавления переменных
2. Нажмите "Deploy ⇧+Enter" (в левой панели, фиолетовая кнопка)
3. Или Railway автоматически начнет деплой

## 📋 Чеклист

- [ ] DATABASE_URL настроен как `${{Postgres.DATABASE_URL}}`
- [ ] BACKEND_PUBLIC_URL настроен как `${{RAILWAY_PUBLIC_DOMAIN}}`
- [ ] TELEGRAM_BOT_TOKEN удален (не нужен для Backend)
- [ ] REDIS_URL удален (если не используете)
- [ ] Нажата кнопка "✓ Add"
- [ ] Запущен деплой

## ⚠️ Важно

- **TELEGRAM_BOT_TOKEN** нужен только для Bot сервиса, не для Backend!
- **DATABASE_URL** должен быть Reference на Postgres (уже правильно)
- **BACKEND_PUBLIC_URL** должен быть Reference на RAILWAY_PUBLIC_DOMAIN

## 🚀 После деплоя

1. Дождитесь успешного деплоя (зеленая галочка)
2. Скопируйте URL Backend (будет в Networking или Deployments)
3. Этот URL понадобится для настройки Bot и MiniApp

