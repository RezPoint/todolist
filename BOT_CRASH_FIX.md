# 🔧 Исправление: Bot сервис крашится

## ❌ Проблема

Bot сервис показывает статус "Crashed" в Railway Dashboard.

## 🔍 Диагностика

### 1. Проверьте логи Bot сервиса

1. **Откройте Railway Dashboard**
2. **Откройте Bot сервис**
3. **Перейдите в Logs**
4. **Посмотрите последние ошибки:**
   - Скопируйте текст ошибки
   - Обычно ошибка в конце логов

### 2. Проверьте Variables Bot сервиса

**Обязательные переменные:**
```
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
BACKEND_URL=http://todolist:3000
```

**Опциональные:**
```
MINIAPP_URL=https://miniapp-9gv2m0r5q-todolistrezpoins-projects.vercel.app
```

## ✅ Решение

### Проблема 1: Отсутствует TELEGRAM_BOT_TOKEN

**Ошибка в логах:**
```
❌ Invalid environment configuration: {
  TELEGRAM_BOT_TOKEN: [ 'Invalid input: expected string, received undefined' ]
}
```

**Решение:**
1. Bot сервис → Variables
2. Добавьте: `TELEGRAM_BOT_TOKEN=ваш_токен`
3. Сохраните и дождитесь перезапуска

### Проблема 2: Неправильный BACKEND_URL

**Ошибка в логах:**
```
❌ Invalid environment configuration: {
  BACKEND_URL: [ 'Invalid input: expected url, received ...' ]
}
```

**Решение:**
1. Bot сервис → Variables
2. Проверьте `BACKEND_URL`:
   - Должен быть: `http://todolist:3000` (внутренний домен)
   - Или: `http://todolist.railway.internal:3000`
3. Убедитесь что Backend сервис называется "todolist" или используйте правильное имя

### Проблема 3: Backend не доступен

**Ошибка в логах:**
```
Error: connect ECONNREFUSED
Failed to connect to backend
```

**Решение:**
1. Проверьте что Backend сервис работает (зеленая галочка)
2. Проверьте что Backend и Bot в одном проекте Railway
3. Проверьте `BACKEND_URL` - должен использовать внутренний домен

### Проблема 4: Ошибка при запуске бота

**Ошибка в логах:**
```
❌ Failed to start bot: ...
```

**Решение:**
1. Проверьте `TELEGRAM_BOT_TOKEN` - должен быть валидным
2. Проверьте что токен не истек
3. Попробуйте получить новый токен от @BotFather

## 🔧 Общие шаги исправления

1. **Проверьте логи:**
   - Bot сервис → Logs
   - Найдите последнюю ошибку
   - Скопируйте текст ошибки

2. **Проверьте Variables:**
   - Bot сервис → Variables
   - Убедитесь что все обязательные переменные установлены
   - Проверьте формат значений

3. **Проверьте подключение:**
   - Убедитесь что Backend работает
   - Проверьте что Backend и Bot в одном проекте

4. **Redeploy:**
   - Bot сервис → Deployments → "..." → Redeploy
   - Или подождите автоматического перезапуска

## ✅ Проверка после исправления

1. **Проверьте статус:**
   - Bot сервис должен показать зеленую галочку
   - Статус должен быть "Running"

2. **Проверьте логи:**
   - Должно быть: `🤖 Bot started successfully!`
   - Не должно быть ошибок

3. **Проверьте в Telegram:**
   - Отправьте `/start` боту
   - Должен ответить приветствием

## 📋 Полный список переменных для Bot

```
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
BACKEND_URL=http://todolist:3000
MINIAPP_URL=https://miniapp-9gv2m0r5q-todolistrezpoins-projects.vercel.app
```

## 💡 Важно

- **Backend должен работать** перед запуском Bot
- **Bot и Backend** должны быть в одном проекте Railway
- **BACKEND_URL** должен использовать внутренний домен (не публичный)

