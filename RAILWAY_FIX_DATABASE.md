# 🔧 Исправление ошибки DATABASE_URL

## ❌ Проблема

Backend сервис не может запуститься, потому что отсутствует переменная `DATABASE_URL`:

```
❌ Invalid environment configuration: {
  DATABASE_URL: [ 'Invalid input: expected string, received undefined' ]
}
```

## ✅ Решение

### Способ 1: Подключить Postgres к Backend (Рекомендуется) ⭐

1. **Откройте Railway Dashboard**
2. **Перейдите в Architecture view**
3. **Подключите Postgres к Backend:**
   - Найдите Postgres сервис (обычно называется "Postgres")
   - Найдите Backend сервис (обычно называется "todolist" или "backend")
   - **Перетащите** Postgres к Backend сервису (или нажмите на Postgres → "Connect" → выберите Backend)
   - Railway автоматически добавит `DATABASE_URL` в Backend сервис
4. **Проверьте Variables:**
   - Откройте Backend сервис → Variables
   - Должна появиться переменная `DATABASE_URL` автоматически

### Способ 2: Добавить переменную вручную

**⚠️ ВАЖНО:** Сначала проверьте имя вашего Postgres сервиса!

1. **Проверьте имя Postgres сервиса:**
   - В Architecture view найдите Postgres сервис
   - Запомните его точное имя (может быть "Postgres", "postgres", "PostgreSQL" и т.д.)

2. **Откройте Backend сервис** в Railway
3. **Перейдите в Variables**
4. **Добавьте переменную:**
   ```
   DATABASE_URL=${{Postgres.DATABASE_URL}}
   ```
   - **Если Postgres называется по-другому**, замените "Postgres" на его имя
   - Например: `${{postgres.DATABASE_URL}}` или `${{PostgreSQL.DATABASE_URL}}`
   - Railway автоматически подставит правильный URL

5. **Сохраните и дождитесь перезапуска**

### Способ 3: Если Postgres называется по-другому

Если ваш Postgres сервис называется не "Postgres", используйте его имя:

```
DATABASE_URL=${{Имя_Вашего_Postgres_Сервиса.DATABASE_URL}}
```

## 📋 Переменные для Backend

Backend сервис должен иметь:

```
NODE_ENV=production
PORT=3000
DATABASE_URL=${{Postgres.DATABASE_URL}}
BACKEND_PUBLIC_URL=${{RAILWAY_PUBLIC_DOMAIN}}
```

## 📋 Переменные для Bot

Bot сервис НЕ использует DATABASE_URL! Ему нужны:

```
NODE_ENV=production
TELEGRAM_BOT_TOKEN=ваш_токен
BACKEND_URL=http://todolist:3000
MINIAPP_URL=https://your-miniapp-url.vercel.app
```

## ⚠️ Важно

- **Backend** → нужен `DATABASE_URL` (подключение к Postgres)
- **Bot** → НЕ нужен `DATABASE_URL` (обращается к Backend через API)

## ✅ Проверка

После добавления `DATABASE_URL` в Backend:
1. Railway автоматически перезапустит сервис
2. Проверьте логи - ошибка должна исчезнуть
3. Должно быть: `🚀 Backend ready on http://localhost:3000`

## 🔍 Как проверить что переменная установлена

1. **Откройте Backend сервис → Variables**
2. **Проверьте наличие `DATABASE_URL`:**
   - Должна быть переменная `DATABASE_URL`
   - Значение должно начинаться с `postgresql://` или `postgres://`
   - Если переменной нет или она пустая - добавьте вручную

## ⚠️ Частые ошибки

1. **Неправильное имя сервиса:**
   - Используйте точное имя Postgres сервиса из Architecture view
   - Регистр имеет значение!

2. **Переменная не сохранилась:**
   - Убедитесь что нажали "Save" или "Apply"
   - Проверьте что переменная видна в списке Variables

3. **Postgres не подключен:**
   - В Architecture view должна быть линия между Postgres и Backend
   - Если линии нет - подключите сервисы

