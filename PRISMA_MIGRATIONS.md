# 🔧 Создание таблиц в базе данных (Prisma Migrations)

## ❌ Проблема

В базе данных нет таблиц, хотя Backend запущен. Prisma требует миграции для создания таблиц.

## ✅ Решение

### Способ 1: Запустить миграции через Railway CLI (Рекомендуется)

1. **Установите Railway CLI** (если еще не установлен):
   ```bash
   curl -fsSL https://railway.com/install.sh | sh
   ```

2. **Авторизуйтесь:**
   ```bash
   railway login
   ```

3. **Подключитесь к проекту:**
   ```bash
   railway link
   ```
   Выберите ваш проект "appealing-gentleness"

4. **Выберите Backend сервис:**
   ```bash
   railway service
   ```
   Выберите Backend сервис

5. **Запустите миграции:**
   ```bash
   railway run npm run prisma:migrate:deploy
   ```
   
   Или если команды нет:
   ```bash
   railway run npx prisma migrate deploy
   ```

### Способ 2: Добавить команду в процесс деплоя

1. **Откройте Backend сервис в Railway**
2. **Перейдите в Settings → Deploy**
3. **Найдите раздел "Build Command" или добавьте скрипт:**
   - Добавьте команду миграции перед запуском
   - Или создайте скрипт в `package.json`

4. **Обновите `apps/backend/package.json`:**
   ```json
   {
     "scripts": {
       "postinstall": "prisma generate",
       "migrate": "prisma migrate deploy",
       "start": "node dist/main.js"
     }
   }
   ```

5. **Или добавьте в Start Command:**
   ```
   npm run migrate && npm start
   ```

### Способ 3: Запустить через Railway Shell

1. **Откройте Backend сервис в Railway**
2. **Нажмите на три точки (...) → Shell**
3. **Выполните команды:**
   ```bash
   cd apps/backend
   npx prisma migrate deploy
   ```

### Способ 4: Автоматические миграции при деплое

Добавьте в `apps/backend/package.json`:

```json
{
  "scripts": {
    "postinstall": "prisma generate",
    "migrate:deploy": "prisma migrate deploy",
    "start": "node dist/main.js",
    "start:migrate": "npm run migrate:deploy && npm start"
  }
}
```

И измените Start Command в Railway на:
```
cd apps/backend && npm run start:migrate
```

## 📋 Проверка

После запуска миграций:

1. **Проверьте таблицы в Railway:**
   - Postgres сервис → Database → Data
   - Должны появиться таблицы:
     - `User`
     - `Project`
     - `Task`
     - `ProjectCollaborator`
     - `_prisma_migrations`

2. **Проверьте логи Backend:**
   - Backend должен запуститься без ошибок
   - Не должно быть ошибок подключения к БД

## 🔍 Диагностика

### Если миграции не запускаются:

1. **Проверьте DATABASE_URL:**
   - Backend → Variables
   - Должна быть переменная `DATABASE_URL`
   - Значение должно начинаться с `postgresql://`

2. **Проверьте Prisma Client:**
   - Убедитесь что `prisma generate` выполнен
   - Обычно выполняется в `postinstall`

3. **Проверьте миграции:**
   - В папке `apps/backend/prisma/migrations` должны быть файлы миграций
   - Если нет - создайте: `npx prisma migrate dev --name init`

## 💡 Важно

- **Миграции нужно запустить один раз** при первом деплое
- **После этого** таблицы будут созданы
- **При следующих деплоях** миграции применятся автоматически (если настроено)

## 🚀 Рекомендуемый подход

Добавьте автоматический запуск миграций:

1. **Обновите `apps/backend/package.json`:**
   ```json
   {
     "scripts": {
       "postinstall": "prisma generate",
       "migrate:deploy": "prisma migrate deploy",
       "start": "node dist/main.js",
       "start:prod": "npm run migrate:deploy && npm start"
     }
   }
   ```

2. **Измените Start Command в Railway:**
   ```
   cd apps/backend && npm run start:prod
   ```

3. **Теперь при каждом деплое:**
   - Сначала выполнятся миграции
   - Потом запустится Backend

