# 🔧 Исправление ошибок сборки

## Проблемы

1. **Prisma Client не сгенерирован** перед TypeScript компиляцией
2. **Node.js версия 18**, а нужна 20+ для Vite
3. **Типы не определены** в некоторых местах

## ✅ Исправления

### 1. Prisma Client генерация

Обновлен `apps/backend/package.json`:
```json
"build": "npm run prisma:generate && tsc -p tsconfig.build.json"
```

Теперь Prisma Client генерируется **перед** TypeScript компиляцией.

### 2. Node.js версия

Добавлен `nixpacks.toml` для Railway:
```toml
[phases.setup]
nixPkgs = ["nodejs-20_x"]
```

Обновлен `railway.toml`:
```toml
[build.env]
NODE_VERSION = "20"
```

### 3. Типы

Исправлен тип в `apps/backend/src/routes/tasks.ts`:
```typescript
return tasks.map((task: typeof tasks[0]) => ({
```

### 4. Dockerfile

Обновлен Dockerfile для генерации Prisma Client перед сборкой.

## 🚀 После исправлений

Railway автоматически:
1. Установит Node.js 20
2. Установит зависимости
3. Сгенерирует Prisma Client
4. Соберет проект
5. Запустит Backend

## 📋 Проверка

После деплоя проверьте:
- [ ] Backend успешно собрался
- [ ] Prisma Client сгенерирован
- [ ] Нет ошибок TypeScript
- [ ] Backend запустился

