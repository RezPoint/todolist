# 🔧 Исправление: "prisma" не является командой

## ❌ Проблема

При попытке запустить `prisma migrate deploy` в Railway Shell получаете ошибку:
```
"prisma" не является внутренней или внешней командой
```

## ✅ Решение

### Способ 1: Использовать npx (Рекомендуется)

В Railway Shell выполните:

```bash
cd /app
cd apps/backend
npx prisma migrate deploy
```

### Способ 2: Использовать npm run

```bash
cd /app
cd apps/backend
npm run prisma:migrate
```

### Способ 3: Установить зависимости сначала

Если `npx` тоже не работает:

```bash
cd /app
npm install
cd apps/backend
npx prisma migrate deploy
```

## 📋 Полная последовательность команд

### В Railway Shell:

```bash
# 1. Перейдите в корень проекта
cd /app

# 2. Установите зависимости (если нужно)
npm install

# 3. Перейдите в папку backend
cd apps/backend

# 4. Запустите миграции
npx prisma migrate deploy
```

## 🔍 Альтернатива: Через Railway CLI

Если Shell не работает, используйте Railway CLI:

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Авторизуйтесь
railway login

# Подключитесь к проекту
railway link

# Выберите Backend сервис
railway service

# Запустите миграции
railway run npm run prisma:migrate
```

## ⚠️ Важно

- **Используйте `npx`** - это запустит prisma из node_modules
- **Или используйте `npm run`** - это запустит скрипт из package.json
- **Не используйте просто `prisma`** - он не установлен глобально

## ✅ Проверка

После выполнения миграций:

1. **Проверьте логи:**
   - Должно быть: "Applying migration..." или "Migration applied"

2. **Проверьте таблицы:**
   - Railway → Postgres → Database → Data
   - Должны появиться таблицы

