# ⚙️ Настройка Railway - Детальная инструкция

## 🔧 Настройка Backend сервиса

### Основные настройки

1. **Root Directory**
   - Установите: `/` (корень проекта)
   - ✅ Уже установлено правильно

2. **Start Command** (ВАЖНО!)
   - Нажмите на поле "Start Command"
   - Введите: `cd apps/backend && npm start`
   - Это запустит Backend после сборки

3. **Build Command** (опционально)
   - Можно оставить пустым (Railway автоматически определит)
   - Или установить: `npm install && npm run build --workspace=apps/backend`

4. **Builder**
   - Оставьте "Railpack" (по умолчанию)
   - Railway автоматически определит Node.js проект

### Переменные окружения (Variables)

Нажмите на вкладку **"Variables"** и добавьте:

```
NODE_ENV=production
PORT=3000
DATABASE_URL=${{Postgres.DATABASE_URL}}
BACKEND_PUBLIC_URL=${{RAILWAY_PUBLIC_DOMAIN}}
```

**Как добавить:**
1. Нажмите "+ New Variable"
2. Введите имя переменной (например: `NODE_ENV`)
3. Введите значение (например: `production`)
4. Для `DATABASE_URL` используйте Reference: выберите вашу PostgreSQL базу данных
5. Для `BACKEND_PUBLIC_DOMAIN` используйте Reference: `${{RAILWAY_PUBLIC_DOMAIN}}`

### Networking

1. **Public Networking**
   - ✅ Включите (должен быть включен)
   - Это позволит Backend быть доступным извне

2. **Generate Domain**
   - Нажмите "Generate Domain"
   - Railway создаст публичный URL (например: `backend-production-xxxx.up.railway.app`)
   - Скопируйте этот URL - он понадобится для Bot и MiniApp

### Restart Policy

- Оставьте "On Failure" (по умолчанию)
- Max restart retries: 10 (по умолчанию)

### Resource Limits

- CPU: 2 vCPU (по умолчанию, можно оставить)
- Memory: 1 GB (по умолчанию, можно оставить)

## 🔧 Настройка Bot сервиса

После настройки Backend, создайте Bot сервис:

1. **Создание сервиса**
   - В том же проекте нажмите "+ New"
   - Выберите "GitHub Repo"
   - Выберите тот же репозиторий `RezPoint/todolist`

2. **Настройки Bot:**
   - **Root Directory**: `/`
   - **Start Command**: `cd apps/bot && npm start`
   - **Public Networking**: ❌ НЕ включайте (бот не нужен публично)
   - **Variables:**
     ```
     NODE_ENV=production
     TELEGRAM_BOT_TOKEN=ваш_токен_бота
     BACKEND_URL=https://backend-production-xxxx.up.railway.app
     MINIAPP_URL=https://ваш-miniapp-url.vercel.app
     ```

## 📋 Чеклист настройки

### Backend
- [ ] Root Directory: `/`
- [ ] Start Command: `cd apps/backend && npm start`
- [ ] Public Networking: ✅ Включено
- [ ] Domain сгенерирован
- [ ] Variables добавлены:
  - [ ] `NODE_ENV=production`
  - [ ] `PORT=3000`
  - [ ] `DATABASE_URL=${{Postgres.DATABASE_URL}}`
  - [ ] `BACKEND_PUBLIC_URL=${{RAILWAY_PUBLIC_DOMAIN}}`

### Bot
- [ ] Root Directory: `/`
- [ ] Start Command: `cd apps/bot && npm start`
- [ ] Public Networking: ❌ Выключено
- [ ] Variables добавлены:
  - [ ] `NODE_ENV=production`
  - [ ] `TELEGRAM_BOT_TOKEN=ваш_токен`
  - [ ] `BACKEND_URL=[URL из Backend]`
  - [ ] `MINIAPP_URL=[URL из Vercel]`

## ✅ После настройки

1. Railway автоматически начнет деплой
2. Дождитесь успешного деплоя (зеленая галочка)
3. Скопируйте URL Backend
4. Обновите `BACKEND_URL` в Bot сервисе
5. Обновите `VITE_BACKEND_URL` в Vercel

## 🐛 Решение проблем

### Ошибка при сборке
- Проверьте, что `package.json` в корне проекта
- Проверьте, что все зависимости установлены

### Ошибка при запуске
- Проверьте Start Command: должен быть `cd apps/backend && npm start`
- Проверьте переменные окружения
- Проверьте логи в Railway Dashboard

### Backend не отвечает
- Проверьте Public Networking (должен быть включен)
- Проверьте, что Domain сгенерирован
- Проверьте Healthcheck Path (можно установить `/api/health`)

