# 📱 Настройка Telegram MiniApp

## 🎯 Что такое MiniApp?

MiniApp - это веб-приложение, которое открывается прямо в Telegram. Оно предоставляет более удобный интерфейс для управления задачами с красивым UI.

## 📋 Текущий статус

✅ MiniApp создан и готов к использованию
- React + Vite
- Tailwind CSS для стилей
- Telegram WebApp SDK
- Zustand для управления состоянием
- Интеграция с Backend API

## 🚀 Как добавить MiniApp в бота

### Вариант 1: Локальное тестирование (ngrok)

1. **Установите ngrok:**
   ```bash
   # Скачайте с https://ngrok.com/download
   # Или через npm: npm install -g ngrok
   ```

2. **Запустите MiniApp:**
   ```bash
   npm run dev -w apps/miniapp
   ```

3. **Создайте туннель ngrok:**
   ```bash
   ngrok http 5173
   ```

4. **Скопируйте HTTPS URL** (например: `https://abc123.ngrok.io`)

5. **Настройте переменную окружения:**
   ```bash
   # В apps/bot/.env добавьте:
   MINIAPP_URL=https://abc123.ngrok.io
   ```

6. **Перезапустите бота:**
   ```bash
   npm run dev -w apps/bot
   ```

### Вариант 2: Продакшн (хостинг)

1. **Соберите MiniApp:**
   ```bash
   npm run build -w apps/miniapp
   ```

2. **Загрузите на хостинг:**
   - Vercel, Netlify, или любой другой статический хостинг
   - Или используйте свой сервер с nginx

3. **Настройте URL в боте:**
   ```bash
   # В apps/bot/.env:
   MINIAPP_URL=https://your-domain.com
   ```

4. **Настройте Bot через @BotFather:**
   - Откройте @BotFather в Telegram
   - Отправьте `/mybots`
   - Выберите вашего бота
   - Выберите "Bot Settings" → "Menu Button"
   - Отправьте URL вашего MiniApp

## 🔧 Настройка через @BotFather

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота (@todolistrezpoinbot)
4. Выберите "Bot Settings"
5. Выберите "Menu Button"
6. Выберите "Configure Menu Button"
7. Введите текст кнопки (например: "📱 Открыть задачи")
8. Введите URL вашего MiniApp (HTTPS обязательно!)

## 📝 Структура MiniApp

```
apps/miniapp/
├── src/
│   ├── App.tsx           # Главный компонент
│   ├── components/       # React компоненты
│   │   ├── TaskList.tsx
│   │   ├── TaskItem.tsx
│   │   └── AddTaskForm.tsx
│   ├── store/            # Zustand store
│   │   └── taskStore.ts
│   └── lib/              # API клиент
│       └── api.ts
```

## 🎨 Возможности MiniApp

- ✅ Красивый современный UI
- ✅ Группировка задач по статусам
- ✅ Создание, редактирование, удаление задач
- ✅ Изменение статусов задач
- ✅ Адаптивный дизайн
- ✅ Работает в Telegram WebView

## 🔒 Безопасность

MiniApp использует Telegram WebApp SDK для:
- Автоматической авторизации через `initData`
- Безопасной передачи данных пользователя
- Интеграции с Telegram интерфейсом

## 🧪 Тестирование

1. Запустите все сервисы:
   ```bash
   # Terminal 1: Backend
   npm run dev -w apps/backend
   
   # Terminal 2: Bot
   npm run dev -w apps/bot
   
   # Terminal 3: MiniApp
   npm run dev -w apps/miniapp
   ```

2. Настройте ngrok для MiniApp

3. Откройте бота в Telegram и нажмите кнопку "📱 Открыть MiniApp"

## 📚 Дополнительная информация

- [Telegram Mini Apps Documentation](https://core.telegram.org/bots/webapps)
- [Telegram WebApp SDK](https://github.com/twa-dev/sdk)

